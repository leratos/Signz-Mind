{
  "name": "ki-editor-api",
  "version": "0.1.0",
  "main": "main.py",
  "scripts": {
    "start": "../venv/bin/python ../venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1"
  },
  "engines": {
    "node": ">=18" 
  }
}